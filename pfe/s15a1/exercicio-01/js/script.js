const header = document.querySelector('.header');

header.addEventListener('click', function() {
    const novaCor = geraCorAleatória();
    header
});

function mudarCor() {
    let cor = '#';
    let letras = '0123456789ABCDEF';
    for
}